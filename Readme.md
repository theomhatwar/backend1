# Chai aur Backend series

This is a video series on backend with JS
- [Models Link](https://www.youtube.com/watch?v=9B4CvtzXRpc&t=1622s)

 "scripts": {
    "dev": "nodemon -r dotenv/config --experimental-json-modules src/index.js"
  }, 
   
used for older node versions=> --experimental-json-modules

-r dotenv/config => used when we do not use [import dotenv from "dotenv"].